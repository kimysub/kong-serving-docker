# =============================================================================
# Kong Gateway Docker Compose — Environment Variables
# =============================================================================
# Copy this file to .env and adjust values as needed:
#   cp .env.example .env
# =============================================================================

# -- Kong version -------------------------------------------------------------
KONG_VERSION=3.9.1

# -- PostgreSQL ---------------------------------------------------------------
KONG_PG_DATABASE=kong
KONG_PG_USER=kong
# Password is stored in the POSTGRES_PASSWORD file (Docker secret).

# -- Host port mappings -------------------------------------------------------
KONG_PROXY_PORT=8000
KONG_PROXY_SSL_PORT=8443
POSTGRES_PORT=5432

# -- LLM backend URLs --------------------------------------------------------
# vLLM upstream — default assumes vLLM runs on the Docker host at port 8080.
# NOTE: Kong proxy also defaults to port 8000, so if vLLM uses 8000 you must
#       change one of them. See README for details.
VLLM_UPSTREAM_URL=http://host.docker.internal:8080

# SGLang upstream — default assumes SGLang runs on the Docker host at port 30000.
SGLANG_UPSTREAM_URL=http://host.docker.internal:30000

# -- Admin API key ------------------------------------------------------------
# Used by the key-auth plugin to protect the /admin-api route.
# CHANGE THIS in production!
KONG_ADMIN_API_KEY=changeme-admin-key-secret
